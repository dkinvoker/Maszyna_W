sub silnia(liczba):(wynik)
{
	if(liczba == 1)
	{
		wynik = 1;
		return;
	}
	
	int buffer;
	buffer = liczba - 1;


	call silnia(buffer):(wynik);
	wynik = wynik * liczba;

	return;
}

start:
int Liczba;

input(Liczba);

call silnia(Liczba):(Liczba);

output(Liczba);